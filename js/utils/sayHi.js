/**
 * Say Hi to the user on the console
 *
 * @param {string} name - the name for the message
 * @returns The greetings message.
 */
function sayHi(name = 'Dev') {
    return `Hello there ${name}`;
}

export default sayHi;
